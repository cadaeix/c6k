<!-- THE SHARED PAGE: the one file written in two hands — the maker's and the
     character's — each entry dated and signed, strike-throughs left visible, later
     hands answering earlier ones. It exists because a world needs at least one place
     that belongs to the relationship rather than to either party.

     (Its ancestor was born as a bug-fix: the harness leaked the maker's idle cursor
     position into the character's context, the character read the signal as a
     conversational gambit, and the fix was not to suppress the signal but to build a
     café — a place where the cursor canonically RESTS. A machine-room leak, turned
     into furniture. That is the diegetic-interface principle in one anecdote: see
     workshop/docs/00-principles.md.)

     Rename this file to what the place IS in your fiction — a café, a porch, a
     landing, a kitchen table. -->

# The shared page

*(unfurnished)*
